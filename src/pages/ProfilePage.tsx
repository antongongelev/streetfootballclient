import TelegramUserInfo from "../components/TelegramUserInfo";

export default function MapPage() {
    return (
        <div className="page">
            <h1>Профиль</h1>
            <TelegramUserInfo/>
        </div>
    );
}